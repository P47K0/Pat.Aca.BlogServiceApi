{
  "name": "blog-assistant-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker for the AI chat assistant: retrieves grounded context from the KnowledgeBase Cosmos container and generates answers via Workers AI.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
