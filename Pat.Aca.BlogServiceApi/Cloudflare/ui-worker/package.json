{
  "name": "blog-ui-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker serving the server-rendered blog UI: fetches articles from the api-proxy Worker (blog-service-worker) and renders pages with Hono JSX.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.13.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
